services:
  user-webflux:
    build: .
    container_name: user-webflux
    environment:
      - SERVER_PORT=8181
    ports:
      - "8181:8181"
    networks:
      - reactive-ms-network

networks:
  reactive-ms-network:
    external: true